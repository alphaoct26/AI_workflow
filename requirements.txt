# Vercel deployment — minimal dependencies only.
# Heavy packages (duckdb, psycopg2, pandas, google-genai, python-pptx)
# are excluded because they have C extensions that fail on Vercel's build.
# The app serves hardcoded demo data when those packages are unavailable.
Flask>=3.0.0
python-dotenv>=1.0.0
matplotlib>=3.7.0
numpy>=1.24.0
